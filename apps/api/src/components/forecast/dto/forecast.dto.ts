/**
 * Slice A read types.
 *
 * Re-exported from the generated file so components import from their own
 * `dto/` folder rather than reaching into `graphql.generated.ts` directly. The
 * SDL in src/schema/forecast.graphql remains the source of truth.
 */
export type { Forecast, ForecastDay } from "../../../graphql.generated";
